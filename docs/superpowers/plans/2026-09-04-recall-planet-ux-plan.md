# Recall「惑星の中の体験」実装計画

> **実装する人へ:** このファイルは `superpowers:subagent-driven-development`（推奨）または `superpowers:executing-plans` で1タスクずつ進める前提で書いてあります。手順は `- [ ]` のチェックボックスです。

日付: 2026-09-04
状態: **段4a 完了（2026-09-04）。段1・2・3・4b・5 は着手条件待ち**（後述）
設計: [惑星の中の体験](../specs/2026-09-04-recall-planet-ux-design.md)（決定1〜13）
先行資料: [七つの族の芯](../specs/2026-09-03-seven-cores-design.md)／[Recall 定着エンジン設計](../specs/2026-09-02-recall-engine-design.md)

**目的:** 09-04 設計の決定1〜13 を実装し、いまの球の Recall を惑星の Recall へ差し替える。

**方針:** 段を5つに分け、前の段が動いてから次へ進む。段1〜4 は `RecallScreen` を差し替えないので、
いま動いている球の Recall を壊さずに積み上がる。差し替えは段5でだけ行う。

**技術:** Next.js App Router / React 18 / Canvas 2D / Supabase / Vitest

---

## 全体の制約

- **テストは DOM を持たない。** `vitest.config.ts` に `environment` の指定が無く、`@testing-library/*` も入っていない。
  したがって**判断のロジックはすべて純関数に出し、その純関数をテストする**。canvas の描画そのものはテストせず実機で見る。
  この計画で新しく足す純関数は、既存の `layout.ts` / `srs.ts` と同じく**描画を知らない**こと
- **決定1〜13 を実装で動かさない。** 迷いが出たら設計書に戻る。設計書に無いことは決めずに、この計画の「実装時に確認すること」へ足す
- **線画。** 面・塗り・影を使わない。光（`INK_HALO`）は動きと離れかけにだけ
- **画面に出す語は「深く残した／残した／離れかけ／読んだ／未着手」**（決定9）。「定着」は画面に出さない。
  内部名 `settled`・DB列・`SETTLED_MIN_DAYS` は据え置き
- **文言の禁止語彙を守る。**「振る」「拾う」「血肉」「落ちる」は UI 文言に使わない（外へ「離れる」）
- **記録の意味を変えない。** `recall_progress` / `recall_section_reads` / SRS の段は一切触らない。この計画は見せ方だけ
- **動きを減らす設定**（`useReducedMotion`）で、芯の動き・明滅・弧・カメラの飛びを止め、位置の変更は即時にする
- ダッシュ「——」を使わない

---

## 0. 着手条件

**段1・2・3・4b・5 は、惑星の実装一式が main に入るまで着手できない。**

2026-09-04 に確認した実測:

| 対象 | 状態 |
|---|---|
| `feat/genre-seats-37` | `be69379` で main にマージ済み。**正は main で確定** |
| `worktree-recall-seven-cores` | origin に無い |
| `field.ts` / `field-render.ts` / `cores.ts` | **どの ref にも存在しない**（オーナーの手元の worktree にだけある） |

したがって設計書の段5着手条件「どちらを正にするか決まっていること」は**すでに答えが出ている**。
残っているのは惑星の実装一式を main へ持ってくることだけで、それが済むまで
段1（直す対象が `field-render.ts`）にも手が付けられない。

- [ ] **オーナー作業:** `worktree-recall-seven-cores` の `field.ts` / `field-render.ts` / `cores.ts` を main へ入れる
- [ ] 入ったら、下の「実装時に確認すること」を現物に当てて答えを埋めてから段1へ進む

**段4a はこの条件と無関係に着手できる**（`srs.ts` と `notice.ts` しか触らないため）。
push を待つ間に進めておくと、段4b が来たときに繋ぐだけで済む。

---

## 1. 変更対象ファイル

### 新規作成

| ファイル | 責務 |
|---|---|
| `src/lib/recall/field-layout.ts` | **純関数。** 状態と保持力 → 半径・明るさ（居場所5段）。惑星ごとの要約（モヤ・芯の有無・輪郭・離れかけの光）。描画を知らない |
| `src/lib/recall/field-camera.ts` | **純関数。** 段の判定・惑星への倍率・傾きの頭打ち・慣性の減速・境目の名前の出し入れ。描画を知らない |
| `src/lib/__tests__/recall-field-layout.test.ts` | 段1のテスト |
| `src/lib/__tests__/recall-field-camera.test.ts` | 段2のテスト |
| `src/lib/__tests__/recall-field-angle.test.ts` | 段3のテスト |

### 変更

| ファイル | 変更 |
|---|---|
| `field-render.ts` | 半径・明るさ・要約を `field-layout.ts` から受け取る。`ClaimDot.state` を使い `seen` を廃止。境目の円と名前。芯の手回し |
| `field.ts` | `FieldView` に `focus`。`angleOf`（席→記事→節順）。B の透視投影 `projectInside`。`frontSlotOf` を A・B で共有 |
| `src/lib/recall/srs.ts` | `pickCandidates` に席の絞り込みを足す（既定は現行どおり全席） |
| `src/lib/recall/notice.ts` | `checkNotice` を惑星単位の文言に |
| `src/components/recall/RecallScreen.tsx` | 段5で field へ差し替え。視点A/B の切り替えを操作列に追加 |
| `src/components/recall/RecallSphere.tsx` | 段5で退役 |

---

## 実装時に確認すること（現物が来たら埋める）

設計書が範囲だけを示していて、式を決めていない箇所。**実装者が黙って決めない。**

1. **保持力 → 半径の写像。** 設計書の表は「残した 1.3〜2.6（＝保持力）」「離れかけ 2.6〜2.85」。
   境目の 0.28 で連続にするなら次の式になる。この式でよいか確認する。
   - 残した（`rem` 1→0.28）: `r = 1.3 + 1.3 × (1 − rem) / 0.72`
   - 離れかけ（`rem` 0.28→0）: `r = 2.6 + 0.25 × (0.28 − rem) / 0.28`
   - `rem = 0.28` でどちらも 2.6 になる（境目が飛ばない）
2. **モヤの濃さ。** 37席のうち22席が空なので遠景の6割がモヤになる（09-02 実測で使用中15席）。
   ラフで濃さを振れるようにして実機で決める。定数1か所に置く
3. **`ClaimDot` の現物の形。** `seen` / `rem` 以外に何を持っているか。`state` を足すときに既存の呼び出し側が何箇所あるか
4. **`RING_PITCH` と現行の慣性の有無。** 段2で足す `coast` が既存の回転と二重にならないか

---

## Task 段4a: 席で絞る候補と、惑星単位の文言（**完了・2026-09-04**）

`srs.ts` と `notice.ts` しか触らないので、惑星の実装が来る前に終わらせておける。
決定2「惑星ごとに確かめる」の純関数側。

- [x] **Step 1: 失敗するテストを書く**
  - `src/lib/__tests__/recall-srs.test.ts` に足す
    - 席を指定すると、その席の主張の記録だけが候補になる
    - 席を指定しても、並びは保持力の昇順・最大5のまま（現行の不変条件を壊さない）
    - 席を指定しないときは現行と同じ結果になる（既存テストが通り続ける）
    - 指定した席に離れかけが1件も無ければ空を返す
  - `src/lib/__tests__/recall-notice.test.ts` に足す
    - 候補0の惑星で「この惑星に、いま確かめる主張はありません。次は○日後に○件」
    - 期限切れがあるときは日数を出さない（現行の二重の歯止めを惑星単位でも守る）
    - 「落ちる」を使わない
- [x] **Step 2: 失敗を確認する** … 実装を外した状態で8件が落ちることを確認した。`./node_modules/.bin/vitest run src/lib/__tests__/recall-srs.test.ts src/lib/__tests__/recall-notice.test.ts`
- [x] **Step 3: 実装する**
  - `pickCandidates(progress, now, max)` に第4引数で席の絞り込みを足す。
    記録は `claimId` しか持たないので、**席は呼び出し側が `claimId → genreSlot` の対応を渡す**
    （`srs.ts` に主張コーパスを持ち込まない。純関数のまま保つ）
  - `checkNotice` に席名を渡せるようにする。席を渡さないときは現行の文言のまま

**計画からの意図的な追加（2026-09-04）**: `nextDue` にも同じ `SeatFilter` を足した。
計画は `pickCandidates` と `checkNotice` しか挙げていなかったが、`nextDue` を絞らないと
惑星単位の「次は◯日後に◯件」が**全席の件数**になり、開いている惑星と画面の言葉が食い違う。

**実装の記録**
- `SeatFilter = { slot, slotOf }` を `srs.ts` に置いた。記録は `claimId` しか持たないので、
  席の対応は呼び出し側が渡す（`srs.ts` に主張コーパスを持ち込まない）
- `slotOf` が `undefined` を返す主張（同期で外れた等）は、席を指定したとき候補にしない。
  どの惑星のものか決まらないものを、たまたま開いている惑星の輪から離すのは誤りのため
- 惑星の文言では「球」を出さない（差し替え後に、いない物を指す言葉が残らないようにする）
- 段4b は `claimId → genreSlot` の対応を作って渡すだけでよい。`claims` にある `genreSlot` をそのまま使える
- [x] **Step 4: テストを通す** … recall 関連は 17ファイル・210件が緑
- [x] **Step 5: Commit** … `feat(recall): 席で絞った確かめる候補と、惑星単位の文言`

---

## Task 段1: 居場所5段と要約・空の惑星のモヤ

決定1・10・11。設計書「現行との差分（実装で直すもの）」の3点を含む。

- [ ] **Step 1: 失敗するテストを書く**（`recall-field-layout.test.ts`）
  - 5つの状態が設計書の表どおりの半径に来る（深く残した 1.16 ／ 残した 1.3〜2.6 ／ 離れかけ 2.6〜2.85 ／ 読んだ 3.05 ／ 未着手 3.38）
  - 保持力 0.28 ちょうどの境目で半径が飛ばない（確認事項1の式）
  - **明るさは保持力に比例する**（`0.5 + 0.45 × rem`）。保持力が高いほど明るい。
    現行の `0.42 + 0.34 × (1 − rem)` は忘れるほど明るく、設計書と逆なので、これを回帰テストとして固定する
  - 離れかけの半径が 2.85 を超えない（読んだの帯 3.05 と当たらない）
  - 惑星の要約: 主張0件の席は `haze` が真・`core` が偽・輪郭を描かない
  - 惑星の要約: 主張はあるが全部未着手の席は `haze` が偽・`core` が真・輪郭は最も薄い
    （**空の惑星と未開拓の惑星を取り違えない**。モヤが付くのは前者だけ）
  - 離れかけがある惑星は、光の点が離れかけの数だけ付く（上限5）
- [ ] **Step 2: 失敗を確認する**
- [ ] **Step 3: 実装する**
  - `field-layout.ts` を新規に作り、半径・明るさ・要約を純関数で出す
  - `ClaimDot` に `state`（cold／touched／kept／settled）を足し、`seen` を廃止する
  - `field-render.ts` は `field-layout.ts` を呼ぶだけにする。式を持たない
  - 空席のモヤ: **塗らない。** 輪郭線を1本も引かず、短い線か点を惑星のあるべき範囲に薄く散らす。濃さは定数1か所
- [ ] **Step 4: テストを通す**
- [ ] **Step 5: 実機で見る** … モヤの量（6割）が「これから埋まる場所」に見えるか
- [ ] **Step 6: Commit** … `feat(recall): 居場所5段と、空の惑星のモヤ`

---

## Task 段2: 近景カメラと3段・視点A/B・慣性・境目の名前

決定3・5・6・7。この計画でいちばん大きい段。

- [ ] **Step 1: 失敗するテストを書く**（`recall-field-camera.test.ts`）
  - 段の判定: 倍率4を境に遠景／中景・近景の名前が変わる
  - `zoomForPlanet`: 霧（3.38）が画面の短辺に収まる倍率を返す
  - 傾きの頭打ち: −1.35〜0.05 rad を超えない
  - 中景へ戻すと傾きが `RING_PITCH` に戻る
  - 慣性: 押さえて止めてから離したときは減速が始まらない。放したときは減速して**必ず止まる**（0へ収束）
  - 境目の名前: 入って3秒で消える。3秒より前でも最初のドラッグで消える。居場所5段のときだけ出る
  - `frontSlotOf` が視点A と B で同じ席を返す
- [ ] **Step 2: 失敗を確認する**
- [ ] **Step 3: 実装する**
  - `field-camera.ts`（純関数）と、`field.ts` の `focus`・`projectInside`
  - 近景の横ドラッグは**カメラでなく輪と芯を回す**（惑星が画面中央から動かない）。縦は見下ろし角
  - 芯の手回しは `drawCore3D` の `yaw` / `pitch` に足す。**族の動きは止めない**
  - 視点A/B の切り替えを操作列（「すべて／残したものだけ」の隣）に置き、`localStorage` に保存する。
    学習記録ではないので Supabase には置かない
  - 境目の名前は「深く残した／残した／離れかけ／読んだ／未着手」（決定9）
- [ ] **Step 4: テストを通す**
- [ ] **Step 5: 実機で見る** … 回し軸は**2軸で決定済み**（決定12）。ここでは決め直さず、
  再検討ライン（「回したい向きに回らない」が実機で3回出たとき）に当たるかどうかだけ見る
- [ ] **Step 6: Commit** … `feat(recall): 近景カメラと3段・視点の切り替え・境目の名前`

---

## Task 段3: 記事の扇形

決定4。

- [ ] **Step 1: 失敗するテストを書く**（`recall-field-angle.test.ts`）
  - 扇形の角度の幅が、その記事の主張数の比になる
  - 主張の角度は記事の中で**節の順**に並ぶ（現行は乱数）
  - **主張を足しても、既存の記事の中の相対順が変わらない**
    （`recall-layout.test.ts` の「席を追加しても既存の中心が動かない」と同型の不変条件テスト）
  - 記事が増えたとき、既存の記事の扇形の順序が変わらない
- [ ] **Step 2: 失敗を確認する**
- [ ] **Step 3: 実装する** … `field.ts` の `angleOf`（席→記事→節順）、`field-render.ts` の弧と記事名（11px）
  - 記事名タップでレンズ（その記事だけ明るく、他は沈む）。もう一度で解除
- [ ] **Step 4: テストを通す**
- [ ] **Step 5: Commit** … `feat(recall): 輪を記事ごとの扇形に分ける`

---

## Task 段4b: 惑星ごとに確かめる（画面側）

段4a の純関数を画面に繋ぐ。

- [ ] **Step 1: 実装する**
  - 近景に「この惑星を確かめる」。中景で押したときは、正面の惑星に寄ってから同じことをする
  - 離れる動き: 輪の位置から画面下の棚へ、手前で大きくなりながら浅い弧で 900ms。1件ずつ 70ms ずらす。棚は横一列（最大5）
  - 棚のカード: 表は数値だけ伏せた原文（既存の `holes`）→「答えを見る」→ 原文と出典 →「覚えた」「まだ」
  - 覚えた: 光として輪の内側（保持力1）へ戻る。まだ: 棚に残り、輪へは戻らない（間隔1日）
  - 帯の先頭に「すべて」を残す。押すと離れかけのある惑星を順に回る（**1惑星ずつ。混ぜない**）
  - 帯の席名に離れかけの数を光の色で添える
  - 動きを減らす設定: 弧を飛ばさず棚に即時に置く。明滅も止める
- [ ] **Step 2: 実機で見る** … 残す → 確かめる → 覚えた の一周が惑星で通る
- [ ] **Step 3: Commit** … `feat(recall): 惑星ごとに確かめる`

---

## Task 段5: RecallScreen の差し替え

- [ ] **Step 1: 実装する**
  - `RecallScreen.tsx` を field へ差し替え、`RecallSphere.tsx` を**退役させる**（決定13）。
    切り替えで球を残すことはしない。オーナー専用なので、壊れたときの実害が小さい
  - **`RecallScreen.tsx` が現行で抱えている作り込みを落とさない。** 差し替え前に読み直して引き継ぐ:
    - `z-0` を保つ（上げるとタブの並びごと覆って他のタブへ戻れなくなる）
    - ヘッダーの実測（`data-app-header` を `ResizeObserver` で測る）
    - 走らせた `setTimeout` を全部控えて、戻すで確実に消す
    - 保存に失敗しても操作を止めない。失敗したら主張を棚に戻す
    - カードは Esc と閉じるで、答えずに抜けられる
  - 凡例の「定着した」を「深く残した」に直す（`RecallScreen.tsx:190`。決定9の2箇所目）
- [ ] **Step 2: 実機で見る** … 残す → 確かめる → 覚えた の一周が本番で通る。他のタブへ戻れる
- [ ] **Step 3: Commit** … `feat(recall): 画面を球から惑星へ差し替える`

---

## Task 段6: リスト表示（**未決。オーナーの判断待ち**）

「絵ではなく文字で見たい」利用者のための、惑星と同じ内容のリスト。**まだ決まっていない。**
段5まで終えてから着手する想定なので、この判断で段1〜5は止まらない。

### Claude Code の推奨: 入れる。ただし主要ナビにしない

理由が3つある。

1. **canvas は支援技術に何も出さない。** 惑星も球も、読み上げには存在しない画面になる
2. **明るさから記憶の状態は読み取れない。** 不透明度は「状態 × 光の当たり × 奥行き × 保持力」の積で、
   保持力による差（約1.8倍）は奥行き（約8.3倍）や光（約3.8倍）より小さい。
   引きで全体の面積を眺める用途では問題にならないが、「どれが離れかけか」を目で確かめる手段が無い。
   **「次は◯日後」を数字で出す場所**が要る
3. 低性能端末の代替になる

一方で、リストを主要ナビゲーションに据えると惑星が装飾に落ちる。**既定は惑星、リストは切り替え**にする。

### 構成案

- 席 → 記事 → 主張 の三段。`RecallProvider` が `claims` / `progress` / `reads` を素の配列で持っているので、**データ層の追加はゼロ**
- 各主張に**状態名**（深く残した／残した／離れかけ／読んだ／未着手）と**次の期限**（◯日後）。
  決定9で決めた5語が、ここで初めて列の値として働く
- 並びの既定は「離れかけが先」。席で畳める
- 状態と期限の文字列は**純関数**に出してテストする（DOM が無いため）

### オーナーが決めること

- 入れるか、入れないか
- 入れるなら、段6として段5の後か、もっと先送りか

---

## この計画で扱わないこと

- 週1の便り、Notion への落とし（別部品）
- 読む画面からの「残す」「読んだ」の入口（部品1。**実装済み**。main `f74ae8e`）
- 伏せ字の量を記憶度で変える案（Monoxer 特許の請求項を読んでから）
- 描画層の WebGL 差し替え（主張2,500超で。現在687）
- **公開範囲と課金判定。** `/api/recall/claims` は `hasFeature('recall')` だけを見ており、
  有料記事の主張の原文と出典が全文返る。いまはオーナー専用なので露出していないが、
  公開範囲を広げる前に必要。ロードマップ上は提案009の範囲
