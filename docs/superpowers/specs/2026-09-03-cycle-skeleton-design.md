# 双輪サイクルの骨組み（部品の並び・継ぎ目・回し方）

日付: 2026-09-03
状態: オーナー承認済み（会話で段階確認。案「1周を先に通す」を採用）。各部品の実装計画は部品ごとに別セッションで起こす
先行資料:
- 外の輪 `docs/superpowers/specs/2026-09-02-outer-wheel-design.md`（段0／段1／段2の契約）
- 内の輪 `docs/superpowers/specs/2026-09-02-recall-engine-design.md`（主張・球・確かめる・SRS・テーブル・今日の1問の撤去範囲）
- 双輪の骨格（2026-09-01。正は Notion 🎬プロジェクト_DB「MediNode アプリ開発」配下）

## 背景・課題

内の輪（Recall）は実装が本番に入り、外の輪は設計が承認された。しかし両輪を「一周」として回した人はまだいない。
コードの現物を辿ると、輪は次の箇所で途切れている（2026-09-03 に読み取り専用で確認）。

外の輪（疑問 → 正本）
- 疑問の入口が3つに割れ、返るものが違う。検索はページ着地で終わり、投げ込みは「届きました」で待ち、
  節末の「気になる」は票数だけを返す。段0（棚を引く）は未実装
- 作者側の操作（「対応済み」への変更・ボード公開・「気になる」の集計の目視）はすべて Notion 手作業で、
  アプリは受付DBを読むだけ。書き戻す API が無い
- 正本になった事実がアプリへ戻る経路は、未コミットの通知 cron（`cq-answer-notify`）だけ。
  「依頼者には最初から残した状態で入る」接続点は未実装で、受付DBの行と `recall_claims` を結ぶ列が無い
- 送った疑問の追跡がサーバー側（`cq-mine`）と端末側（`cq-dispatch`）の二重

内の輪（読む → 残す → 確かめる → 定着）
- 読む画面に「残す」が無い。確信度マーク（`ConfidenceMark`）はアイコンだけで押せず、「残す」は球の閲覧カードに閉じている
- 節の読了 API（`/api/recall/read`）に呼び手が無い。「読んだ」の光は一度も点かない
- 残した主張は利用者の Notion に落ちない（設計未着手）。ただし利用者のトークンで DB を新規作成する
  API（`/api/notion/create-db`）が既にあり、OAuth の権限も読み取り＋挿入まで持つ
- クイズタブ（端末保存・赤マーカー母集団）と今日の1問が Recall と並存している

制作工程との接点
- 段2の依頼は受付DBに入るが、正本化した主張を依頼に結ぶ列が無い

本設計は、これらを閉じる部品の並びと、どの部品も勝手に決めてはいけない継ぎ目を固定する。
部品の中身の設計は、必要な部品だけ別セッションで行う。

## 用語

- **主張／残す／読んだ／確かめる／定着**: Recall 設計の定義に従う
- **段0／段1／段2**: 外の輪の設計に従う（棚を引く／AI に組ませる／作者に検証を依頼）
- **正本化**: 依頼された問いが制作工程を経て公開ページの主張になること
- **部品**: 1セッションで設計または実装する単位。本設計では 0〜8 の9個（8 はパーツ調査で追加）
- **継ぎ目**: 複数の部品が同じものを読み書きする箇所。本設計で固定し、部品の設計で変えない

## 目的と成功条件

**目的**: 双輪サイクルを、オーナー1人が本番で一周できる状態にする。すべて先行体験のフラグ（`recall`・`ask_shelf`）の内側で行う。

**成功条件**（オーナーが本番で実測して判定する）

- 内の輪: 記事を読む → 主張行で「残す」→ 球で光る → 翌日「確かめる」→ 覚えた／まだ → 記録が Supabase に残り、
  自分の Notion の DB に主張が1行入る
- 外の輪: 疑問を書く → 段0が主張を返す（無ければ「棚に無い」の1行）→ 依頼 → 受付DBに段0の結果つきで入る →
  オーナーが制作工程で正本化 → サブスク同期で主張が入る → 依頼者にメールが届き、その主張が「残した」状態で球に灯る
- 段1: ボタンで AI 回答が返り、未検証の表示と引用チップが出て、MediNode 側に保存される

**非目標**

- 一般公開（drnode.com の公開判断まで先行体験の内側）
- 見た目の一新（後述の再検討ラインまで凍結）
- クイズタブと Recall の統合（部品6で判断だけ行う）
- 個人・部署ナレッジの主張を球に出すこと（v2）
- 制作スキル（CQ・Essentials・検証層）の中身の変更。本設計が触るのは受付DBの列という接点だけ

## 完成形の体験（オーナー専用の状態で回す1週間）

1. **現場で3秒。** 検索タブの窓に疑問を自然文で書く。上に段0の主張が数件（各行に「残す」と「この節を読む」）、
   下に今までどおりのページ。主張が無ければ「この問いそのものの主張はまだ棚に無い」の1行と近い主張、
   その下に「作者に検証を依頼」と「AI に組ませる（未検証）」
2. **依頼。** 受付DBに「段0結果」つきで入る。受付画面に「票の多い順に、月に数件を正本にします」の1行が出る
3. **閲読。** 主張行の右端の「残す」、節末の「この節を読んだ」。残した行は翌朝、自分の Notion の DB に入っている
4. **確かめる。** 球の「確かめる」で期限の来た主張が最大5枚。伏せ字カードか想起カード。裏は原文と出典
5. **正本が戻る。** 制作工程で正本化 → 同期 → 依頼者にメール。球にはその主張が「残した」で灯るが、
   期限は依頼者が初めて開いた時点から始まる（初見をクイズにしない）
6. **週1の便り。** 「今週確かめる主張が n 件」と傾向1行（「まだ」が多い区画、棚にあって自分が触れていない区画）

一般の利用者は、この期間は今日と同じ画面（フラグの外側）。今日の1問だけが部品6で消える。

## 部品と順番

| 順 | 部品 | 作るもの | 設計セッション | 実装 |
|---|---|---|---|---|
| 0 | `DESIGN.md` | 既存の確定デザイン（Noto Sans JP・常盤 #196B4F・lucide）から、色・字の大きさ・余白・角丸・ボタンとカードの型を1枚に起こす | 不要 | Sonnet 5 |
| 1 | 読む画面の「残す」と「読んだ」 | 主張行に「残す」の切替、節末に「この節を読んだ」。既存の Recall API を呼ぶ。`recall` の内側にだけ描く | 不要（本設計で決める） | Sonnet 5 |
| 2 | 回答通知の仕上げ | 実装済みの cron（`cq-answer-notify`）と `cq-mine` の修正をコミットし、本番で1回手動実行して確認 | 不要 | Sonnet 5 |
| 3 | 聞ける棚の段0と段2 | 検索タブに主張の段、依頼に段0の結果を添える受付DBの列、正本化で依頼者に「残した」を入れる処理、`ask_shelf` フラグ | 要 | Opus 5 |
| 4 | Notion への一方向落とし | 残した主張を利用者の Notion に自動作成した DB へ書く。既存の DB 作成 API を土台にする | 要 | Opus 5 |
| 5 | 段1（AI に組ませる） | ボタンで踏む AI 回答、未検証表示、引用チップ、保存。Notion 接続済みなら 4 の経路で落とす | 要 | Opus 5 |
| 6 | 今日の1問の撤去と週1の便り | 撤去範囲は Recall 設計書の表。入れ替えに週1の便り（件数＋傾向1行）。クイズタブの端末記録を Recall へ移すかをここで判断 | 小 | Sonnet 5 |
| 8 | 改訂の旗（鮮度） | 主張が改訂・撤回されたとき、残していた人に旗を立て、カードを差し替え、SRS の記録は保つ。出典台帳側で撤回・改訂を拾う口も決める | 要 | Opus 5 |
| 7 | 自分のナレッジの入口と出口（記載フォーム＋昇格描画） | 書く側: アプリ内の記載フォームが、テンプレ規約どおりのブロックを利用者の Notion DB に書く（URL の要約・スクリーンショットの文字起こしなど AI の補助つき）。読む側: テンプレに沿ったページをサブスクのスプレッドと同じ見え方で描く。降格式が既定 | 要 | Opus 5 |

順番の根拠
- 0 は以後の全部品が見た目の基準にするので最初
- 1 と 2 は土台が既にあり、一周の両端を先に繋ぐ
- 3 は正本の紐づけ（受付DBと主張の対応）を決める部品で、4 より前に置かないと、先に残した記録を付け替えることになる
- 5 は費用の実測が要るので、3 で疑問の流れが通ってから乗せる
- 6 は 1〜3 をオーナーが一周した後。撤去だけを先にすると、内の輪を外から引くものが無い期間ができる
- 7 は読む画面の部品を 1 が先に触るので後。2026-08-12 の判断（要点モードは個人ページに出さない・書き方の教育をしない）を
  一部開け直すため、/admin に貯まる計測（ブロック分布・離脱タップ）を見てから設計する

### 各部品の要点

**部品0 `DESIGN.md`**
- 置き場はリポジトリ直下。中身は現行のトークンの写しであり、新しい見た目を決めない
- 実装セッションはこれを読んでから画面を足す

**部品1 読む画面の「残す」と「読んだ」**
- 対象はサブスクのアプリ内リーダー（`ReaderBody` と `ReaderSpread`）。個人・部署ページには置かない（v1 の主張はサブスク限定）
- 「残す」を置く行は、主張抽出（`extract-claims.ts` の `splitClaim`）が主張と判定する行だけ。判定は共有の純関数を使い、
  行の本文から `claimIdOf(ページID, 本文)` を計算して `/api/recall/claims` の一覧と突き合わせる。
  一覧に無い行（未同期・除外対象）には置かない
- 「この節を読んだ」は節末の明示ボタン。節の番号は主張抽出と同じ規則（番号付き H2 → `sec{n}`）。
  スクロールでの自動判定はしない（Essentials 1本で71件が「読んだ」になるのを避ける）
- 既存の端末ローカルの読了水位（`ReaderMarksProvider`。続きから読むための位置）は別物として残し、統合しない
- 「残す」に一括操作を置かない（後述の継ぎ目）
- `hasFeature('recall')` が偽の利用者には、ボタンも節末の操作も描かない

**部品2 回答通知の仕上げ**
- 実装済み。`vercel.json` の cron（`/api/cron/cq-answer-notify`）と `cq-mine` の「対応済み」判定を含めてコミットし、
  本番で `CRON_SECRET` 付きで1回手動実行して `mailed` の件数を見る
- 部品3で「正本主張ID」列が入るまでは、メールだけを送る（従来の振る舞い）

**部品3 聞ける棚の段0と段2**（設計セッションで決めること）
- 段0の検索方式（埋め込みの種類・日本語全文検索との併用・更新頻度）。列は `recall_claims` に足す
- 「正本主張ID」を誰が書くか（管理画面の操作か、Notion での手入力か）
- 依頼の受付画面の期待値の文言と、「対応不要」になったときの理由の見せ方
- 空白候補ビュー（受付DBの1ビュー。段0結果＝該当なしで絞る）
- **制作工程との受け渡し**: CQ スキル（`medinode-cq-note`）が受付DBの依頼を入口として読む手順と、正本化したときに
  「対応状態＝対応済み」と「正本主張ID」を書き戻す手順。スキルの中身は変えず、入口と出口の2手順だけを決める
- **音声で疑問を入れる**: 検索タブの窓に音声入力を置くか。置くなら、AI が整えるのは疑問文の形だけで、答えは作らせない

**部品4 Notion への一方向落とし**（設計セッションで決めること）
- DB の自動作成の流れ（既存の `/api/notion/create-db` の流用範囲）と、作成済み DB の ID の保存先（`user_settings` の暗号化設定）
- 書き込みのタイミング（残した直後か、まとめてか）と失敗時の再送
- Notion API の世代（現行の 2022-06-28 固定のままか、2025-09-03 版へ上げるか）。参照実装は SUBSCreen

**部品5 段1**（設計セッションで決めること）
- モデルの選定と費用の記録。表示規約（未検証・引用チップ・由来なし）。保存先のテーブル

**部品6 今日の1問の撤去と週1の便り**
- 撤去範囲は Recall 設計書「今日の1問の撤去範囲」の表のとおり
- 週1の便りは既存のプッシュ基盤（`push_subscriptions`・`push_notify_prefs`）で送る。日次にしない。
  中身は「今週確かめる主張が n 件」と傾向1行。傾向は `recall_review_log`・`recall_progress`・`recall_section_reads` と
  `recall_claims` から計算だけで出す（AI の作文は使わない）
- 傾向で出してよいもの: 区画ごとの「まだ」の率、残した主張の区画の偏り、読んだが残していない節、
  棚にあって自分が一度も触れていない区画。出してはいけないもの: 点数・順位・赤い表示・未解決の数を責める文言
- クイズタブの端末記録（`quiz-srs.ts`・localStorage）を Recall へ移すか捨てるかは、ここで判断する

**部品7 自分のナレッジの入口と出口（記載フォーム＋昇格描画）**（設計セッションで決めること）
- 書く側（記載フォーム）: 利用者が MediNode に適した書き方を自分で覚えなくても済むように、アプリ内のフォームに入力すると
  テンプレ規約どおりのブロックが利用者の Notion DB に書かれ、同じ原本がアプリ内にも描かれる。
  他の AI（ChatGPT など）で得た回答の保存先にもなる（流れていく回答を止める入口）
- フォームの AI 補助は「形を整える」だけに限る。URL を貼れば要約、スクリーンショットを貼れば文字起こし、画像は整形。
  **答えを作らせない。補助で入った文は既定で ❓（未検証）とし、✅ にできるのは出典を伴う行だけ**。
  検証済みのサブスクと同じ見え方で描くからこそ、未検証の行が同じ確信度に見えてはいけない
- v1 はページの新規作成のみ（既存ページの編集は Notion 側で行う。OAuth の権限は読み取り＋挿入まで確認済み、更新は未確認）
- 既存の土台: `/api/notion/create-cq`（利用者のトークンで自分の DB にページを作る）、`/api/notion/create-db`、
  複製テンプレ（設定の `notionDuplicatedTemplateId`）。CQ Catcher（別アプリ）の捕捉経路と役割が重ならないよう、設計で線を引く
- 読む側: 既存の降格式リーダー（`isInAppReaderTarget`・`/api/personal/page`）を既定のまま残し、テンプレ規約に沿うページだけ昇格させる。
  昇格の判定と、昇格したときの見え方（スプレッドとの共通部品）
- 配るテンプレ（複製元）の更新。教育はしない。フォームが規約どおりに書くので、教育の代わりになる
- AI 補助の費用と、画像の Notion への保存方法（ファイルアップロード API の可否）は設計で確かめる（未確認）

## 今の時点で固定する継ぎ目

1. **主張の鍵は1つ。** `claimIdOf(ページID, 本文)`（sha1 先頭24文字。`extract-claims.ts` に実装済み）。
   受付DBの紐づけ列、Notion に落とす行の「MediNode ID」、段0の検索結果、すべてこの値を使う。
   他の部品が独自の ID を作らない。本文改訂で ID が変わる件は Recall 設計の `active=false` 方式に従う
2. **「残した」の記録は `recall_progress` の1本だけ。** 読む画面、球、段0の結果、依頼の正本化、どこから残しても同じ行。
   Notion への落としはこの表の変化を写す一方向
3. **「読んだ」は節ごとの明示操作。** `recall_section_reads` に `sec{n}` で記録する。
   既存の端末ローカルの読了水位とは統合しない
4. **受付DBに足す列は3つ。** 「段0結果」（select: 該当あり／該当なし／未実行）、「段0主張ID」、「正本主張ID」
   （どちらも rich_text。主張の鍵をカンマ区切り）。既存の列は変えない。
   列が無い受付DBでも既存動線が壊れないこと（`cq-submit` の `buildIntakeProperties` は無い列を積まない）
5. **正本化の合図は「対応状態＝対応済み」かつ「正本主張ID が空でない」。** 通知 cron がこの2つを見て、
   依頼者の `recall_progress` に「残した」を書き（`kept_at` は正本化の時刻、`due_at` は null）、メールを送る。
   **`due_at` は依頼者がその主張か節を初めて開いた時点で「間隔1日・期限翌日」に設定する**（初見をクイズにしない）。
   メールと球のタップ先は読む画面の該当節。正本主張ID が空なら従来どおりメールだけ。
   フラグが閉じている依頼者にも `recall_progress` は書いてよい（画面には出ない。開いたときに最初から灯る）。
   メール文面に Recall の名前を出さない
6. **機能フラグは `recall`（既存）と `ask_shelf`（新設）。** どちらも専用の許可メール（`RECALL_EMAILS`／`ASK_SHELF_EMAILS`）で、
   フォールバック無し。部品1は `recall` の内側、段0・段1・依頼は `ask_shelf` の内側。判定はサーバーが正で、
   費用が出る段1は閉じていれば処理に入らせない。全員開放の GA env は公開判断まで置かない
7. **段0は `recall_claims` を引き、次の3つを必ず満たす。**
   (a) 検索用の列は同じ表に足し、別の倉庫を作らない。
   (b) **利用者自身が残した主張（`recall_progress` に有効な行がある主張）を最上位に返す**。
   (c) 各主張に「この節を読む」を置き、読む画面の該当節へ跳べる（主張を新しい行き止まりにしない）。
   段0が該当なしのときの「棚に無い」の1行は外の輪の設計のとおり
8. **検索タブの見せ方。** 窓は今のまま。主張があるときだけ上に主張の段が開き（折りたたみ可）、下に今までどおりのページ。
   CQ 投げ込みのフォームは独立した入口をやめ、結果の末尾の「作者に検証を依頼」になる。
   節末の「気になる」は読む画面に残す。タブの構成（検索・新着・ジャンル・文献・クイズ・マニュアル・Recall）は変えない
9. **無料／有料の線。** 主張の本文は有料。無料の利用者に段0が返してよいのは、題名・節名・件数と「棚に無い」の1行まで。
   オーナー専用の期間は判定を実装しなくてよいが、公開時に実装が要ることを段0の設計に書く
10. **「残す」に一括操作を置かない。** 71行を一括で残せると、2026-07 の「監修ナレッジ全文配布の却下」に実質抵触し、
    翌日71枚の宿題にもなる。API は既存のレート制限に載せる
11. **Notion に落とす行の列。** 主張／出典／分野／残した日／自分の一言／状態／MediNode ID（＝主張の鍵）。
    利用者側の編集は読まない（v1）
12. **テンプレの書き方の規約は、主張抽出とスプレッドが既に頼っている規約と同じにする。**
    番号付き H2 の節、✅⚠️❓の主張行と出典、⚡結論。部品7の記載フォームはこの規約どおりのブロックを書き、昇格描画はこの規約で判定し、
    v2 で自分のページの主張を球に出すときも同じ抽出器を使う。**規約の正は1か所**（`extract-claims.ts` の判定と、それを説明する文書）で、
    フォーム・描画・抽出が別々の規約を持たない。フォームの AI 補助で入った行は既定で ❓ とし、出典を伴う行だけ ✅ にできる
13. **今日の1問は部品6まで残す。** 撤去範囲は Recall 設計書の表のとおり
14. **見た目は部品0の `DESIGN.md` を基準にし、一新は凍結する**（次節）

## 見た目の扱い

- **見た目の方向は未決。** 凍結ではない（2026-09-03 オーナー訂正）。候補は2つ。現行（Noto Sans JP・常盤 #196B4F・lucide）を保つ案と、
  Node Field 統合LPのスタイルに寄せる案。常盤色は両案で残す前提。
  Node Field 側の現在の顔は「紺×白×群青・角丸なし・Archivo／Zen Kaku Gothic New／IBM Plex Mono・写真はデュオトーン・罫で継ぐ索引」で、
  2026-08-27 には MediNode と意図的に別の顔にすると決めていた。寄せる案を採るなら、この判断を開け直すことになる。
  常盤をこのパレットの中でどう置くかは、比較キャンバスで実物を並べて決める
- 方向が決まるまでは `DESIGN.md`（部品0）を現行の写しとして置き、新しい画面はそれに合わせる。方向が決まったら `DESIGN.md` を差し替え、
  以後の部品はその版に合わせる。既に作った画面の置き換えは棚卸しの1件として扱う
- 決める場は、部品3まで着地してオーナーが一周したあと。全画面の棚卸し（brushup。読み取り専用で候補7件、1件ずつ承認）と、
  同じ3画面（ホーム・読む画面の主張行・Recall）を現行案と Node Field 寄せ案で並べた設計キャンバスを作り、見比べて決める
- 決めるのを前倒しする条件: 一周の実測でオーナー自身が操作に迷った箇所が3つ以上出たとき、または drnode.com の公開判断が先に来たとき

## 議論で見つけた矛盾と処置（2026-09-03）

| # | 矛盾・弱点 | 処置 |
|---|---|---|
| 1 | 正本が戻ったとき、字義どおりだと初見が伏せ字カードになる | 継ぎ目5。`due_at` は初回閲覧から |
| 2 | 段0の主張が新しい行き止まりになる | 継ぎ目7(c)。「この節を読む」必須 |
| 3 | 棚が小さいあいだ段0はほぼ「無い」と答え、段1が実質の入口になって AI 検索と同じ土俵に立つ | 段1はオーナー専用で実測。継ぎ目7(b) で自分の残した主張を最上位に |
| 4 | 今日の1問と朝の通知を廃止すると、内の輪を外から引くものが無くなる | 部品6に週1の便りを同梱（日次にしない） |
| 5 | 段0の無料／有料の線が未定義 | 継ぎ目9 |
| 6 | 依頼の待ち時間に期待値が無く、投げっぱなしが再発する | 受付画面に期待値の1行、「対応不要」の理由表示。部品3で文言を決める |
| 7 | 「残す」の一括操作が凍結事項と Anki 地獄の両方に触れる | 継ぎ目10 |
| 8 | 想起カードは同じ節の主張が同じ表になり見分けられない | Recall セッションへ申し送り（冒頭の一句を手がかりに出す案）。本設計の外 |
| 9 | 「この節を読んだ」を押す動機が弱い | 矛盾ではなく賭け。一周の実測で押したかを見る |
| 10 | 一般利用者には、公開まで今日の1問が消えるだけの期間ができる | 実測で利用がほぼ無いため受け入れる。部品6を公開判断と同時にする選択肢は残す（オーナー判断） |

## セッションの回し方

- 1部品＝1セッション。順番どおりに進め、同じ main に2本を走らせない
- 設計が要る部品（3・4・5・7）は brainstorming → 設計書（`docs/superpowers/specs/`）→ 実装計画（`docs/superpowers/plans/`）。
  要らない部品（0・1・2・6）は実装計画から入る
- 実装はサブエージェント方式（実装 Sonnet 5・レビュー Opus 5）。worktree 必須
- テストは自作の文字列でなく実データを通す（`.preview/` の主張コーパス、受付DBの実物、本番の cron の手動実行）
- push は毎回承認。公開リポジトリなので事業数値を書かない
- 完了条件は「成功条件の該当行をオーナーが本番で実測した」。実装が終わっただけでは閉じない
- 引き継ぎは `~/.claude/handoff/app.md`（非公開）と Notion「MediNode アプリ開発」の申し送り欄

**着手条件**

| 部品 | 始めてよい条件 |
|---|---|
| 0・2 | いつでも |
| 1 | Recall でサブスク同期を1回押して `recall_claims` に主張が入り、残す→確かめる→覚えた の一周が本番で通っている |
| 3 | Recall セッションが同期側（主張の抽出・穴・承認）のコードを触り終えている |
| 4 | 部品3で「正本主張ID」の書き手が決まっている |
| 5 | 部品3が本番で動いている |
| 6 | 部品1〜3をオーナーが一周した後 |
| 7 | 部品1の後。/admin の個人・部署リーダーの計測を見てから。書く側と読む側は同じ設計セッションで扱う |
| 8 | 部品3の後（同期側と出典台帳側の両方に触るため）。実際に改訂が1件起きたときを目安にする |

## Recall セッションへの申し送り

- 想起カードに冒頭の一句（最初の読点まで）を手がかりとして出す案（矛盾8）
- `due_at` が null の `recall_progress` 行を「残したが未開封」として扱えるようにする（継ぎ目5の前提。
  状態計算で候補から外し、球では「残した」の色で灯す）

## 本設計で決めないこと

- 段0の検索方式、正本主張ID の書き手、受付画面の文言（部品3）
- Notion DB の作成の流れと API 世代（部品4）
- 段1のモデル・費用・保存先（部品5）
- クイズタブの端末記録の扱い（部品6）
- 記載フォームの項目と AI 補助の範囲（音声入力を含む）、昇格の判定と見え方、テンプレの更新内容、CQ Catcher との線引き（部品7）

## 参考にした外部サービスと、反映した場所

| 参考 | 借りたもの | 反映先 |
|---|---|---|
| NewMemo（AI 日記） | 声や写真を貼るだけで AI が形を整える | 部品7の記載フォーム（URL の要約・スクショの文字起こし・音声）、部品3の音声入力 |
| NewMemo | 自分の過去の記録に自然文で質問できる | 部品3の段0（自分が残した主張を最上位に返す） |
| NewMemo | 週報・傾向の分析 | 部品6の週1の便り（AI の作文は使わず、数字と区画名だけ） |
| NewMemo | AI の性格を選べる、AI が温かく振り返る | 借りない（キャラ常駐・多人格コーチは却下済み） |
| Refero Styles | 製品ごとの `DESIGN.md` という形式 | 部品0（中身は現行トークンの写し。他社の色や書体は入れない） |
| UI Pocket（Pro） | 「フロー」軸の実例（追加・作成／リクエスト/承認／連携・接続／クイズ・診断） | 各部品の画面を作る直前に、該当フローの実例を3画面だけ見る。設計段階では見ない（語学アプリが多く方向がぶれる） |
- 段0の無料判定の実装時期（公開判断のとき）

## 再検討ライン

- 見た目の一新: 上記「見た目の扱い」のとおり
- 部品の順番: 部品1の着手条件が 2026-10 を過ぎても満たされないとき、部品0・2・3 を先に進める順へ組み替える
- 週1の便り: 4週続けて開封が無ければ、便りの中身を見直す（廃止ではなく中身の見直し）

## パーツ調査（2026-09-03・4本の読み取り調査を骨組みに当てはめた結果）

調べた範囲: 想起・定着（A）、拾う・書く・Notion 落とし（B）、聞ける棚（C）、個人開発と国内の学習・知識アプリ（D）。
各係は実ページで確認した機能だけを報告し、確認できないものは「未確認」と書いた。ここでは骨組みに効くものだけを残す。
価値の判定は「サイクルのどこを良くするか」と「却下事項（毎日型ストリーク・ポイント・キャラ常駐・選択肢式・AI 作成のクイズ文・実名コミュニティ）に触れないか」で行った。

### 骨組みを変えたもの

- **部品8「改訂の旗」を追加する。** 内の輪の骨格（2026-09-01）は「読む → 拾う → 回す → 体得 → 鮮度（改訂で旗）」の5段で、
  骨組みには鮮度の部品が無かった。AnkiHub は監修デッキの修正を承認制で全購読者へ自動配布し（実ページ確認）、scite は引用先が撤回・懸念表明になるとメールで知らせる。
  MediNode の主張は本文改訂で ID が変わり `active=false` になるだけで、残した人に何も届かない。
  部品8: 改訂された主張を残していた人に旗を立て、球のカードを新しい主張に差し替え、SRS の記録（段・間隔）は保つ。原著の撤回・改訂を出典台帳側で拾う口も併せて決める。
  着手条件は部品3の後（同期側と台帳側の両方に触るため）
- **継ぎ目7(b) に印を足す。** 段0で自分が残した主張を最上位に返すだけでなく、「あなたが残した」の印を付ける（Semantic Reader が自分のライブラリに関係する引用を見た目で強調している型）
- **継ぎ目12 に見た目の印を足す。** 記載フォームの AI 補助で入った行は ❓ に加えて、AI 由来と分かる記号を前置する（Readwise Ghostreader は AI 出力をノート欄に隔離し、AI 由来のタグに記号を前置している。
  調査の範囲で「AI が入れた」を保持していた製品はこれだけだった）

### 部品ごとに借りるもの（設計セッションの入力にする）

| 部品 | 借りるパーツ | 出どころ（実ページ確認） | 価値 |
|---|---|---|---|
| 1 残す | 長押しで残す（ボタンを探させない。片手・移動中） | Matter の「highlight without friction」 | 中 |
| 3 段0 | 近い主張を「無理に出さない」閾値を数で決める。類似度が足りなければ出さず、「棚に無い」を言う | Consensus Meter（原文の抜き書きだけで作り、関連論文5本未満なら出さない） | 高 |
| 3 段0 | 「棚に無いときは答えない」をヘルプに仕様として書く | Notion Q&A（一般知識にアクセスしない）、NotebookLM（ソースに無ければ答えない） | 高 |
| 3 段0 | 主張の横に検証者と検証日を出す（誰が引き受けた主張かを名指しできることが差） | Doximity Ask PeerCheck が1.2万人の集合で同じ絵を持つ。差は「1人に固定」 | 中（検証日のデータ有無は未確認） |
| 3 段2 | 依頼画面の期待値は「平均○日・正本化率○%」の枠にし、実測が貯まったら数を入れる | Antaa QA（平均15分・回答率98%以上）、MedPeer（回答率95%以上） | 高 |
| 3 段2 | 板の説明の1行「一人の疑問は多くの疑問かもしれない」。投票の意味を説明する文 | MedPeer FORUM | 中 |
| 3 入口 | 音声で疑問を入れ、あとから文字と出典に戻れる | OpenEvidence Voice Mode、NewMemo | 中 |
| 4 Notion | 自動作成した DB の `data_source_id` を取得して保存する（2025-09-03 版は後方互換なし） | Notion API upgrade guide | 必須 |
| 4 Notion | Notion 未接続の人にも出口を置く（残した主張の Markdown 書き出し） | Glasp（手動コピーと自動 API の併存）、Recall（Markdown 書き出し） | 中 |
| 5 段1 | 引用は文単位。主張由来／出典はあるが位置が特定できない／由来なし の3段階で表示する | Elicit（sentence-level citations）、NotebookLM（ソースが短いと文単位でなく文書全体を参照と明示） | 高 |
| 6 便り | 便りは「メール」と「アプリ内の棚」に分け、メールを止めても中身は残る。曜日を選べる | Readwise（Email Frequency を Never にしてもアプリ内で読める。Themed Review は曜日指定） | 高 |
| 6 傾向 | 傾向の数字は「球のうち暗い主張の割合」「狙った定着率に届いているか」。AI 作文なし | Anki の FSRS 統計（Retrievability の分布・True Retention） | 高 |
| 6 傾向 | 弱い領域を診断名で名指しせず、「まだ」が続く主張そのものを並べる | Duolingo Practice Hub の Mistakes（未確認：実ページ未取得） | 中 |
| 6 便り | 当直・オンコールの曜日に期限を寄せない設定 | Anki の Easy Days | 中 |
| 6 便り | 便りに「偶然の1行」（残した主張から無作為に1つ）を添える。順序の最適化ではなく再会の驚き | mymind の Serendipity、Napkin の Daily Flow、RandoMemory（Notion のページを無作為表示） | 中 |
| 7 フォーム | 必須欄は主張と出典だけに絞り、分野は自動候補にして分類を求めない | Tana のスーパータグ（必須欄を絞り残りは任意）、mymind・Napkin（分類させない） | 高 |
| 7 フォーム | フォームの答えを本文テンプレの穴に差し込んで規約ブロックを組む | Tally → Notion（`@` で回答を page content に差し込む）。Notion Forms はプロパティしか書けず不採用 | 高 |
| 7 フォーム | 「Notion DB をフォームに変え、保存の瞬間に列を埋める」同型の既製品を参照する | Save to Notion | 高 |
| 7 フォーム | 整形の強さを選べ、「整形なし」を常に選べる | Cleft の Clean Transcript | 中 |
| 8 旗 | 承認制で全員に修正を配る。撤回・懸念表明の通知 | AnkiHub、scite Reference Check | 高 |

### Recall セッションへの申し送り（球と確かめるの改良。骨組みの外）

- **明るさを「思い出せる確率」にする。** FSRS の保持率 R(t) = (1 + (19/9)·t/S)^(-1) を、S に現在の間隔日数を入れて計算する。段は固定のままで、状態層の式だけが変わる。
  離脱候補の閾値もこの値で引き直す。価値: 高
- **「覚えた／まだ」を3択にする案。** Readwise Mastery は soon（7日）／Later（14日）／Someday（28日）の3択で、すべて日数ラベル。自己採点の語彙を使わない。
  「もう一度／来週／しばらく先」の形なら選択肢式クイズとは無関係のまま情報量が増える。価値: 高
- **伏せ字の量を記憶度で変える。** 暗い主張は数値1つだけ、明るい主張は上限3まで。原文の伏せ方だけ変えるので AI 作成には当たらない（Monoxer の段階化。出題形式の記述は未確認）。価値: 高
- 1日最大5枚の上限を、内部の「目標保持率」1つのノブに置き換える案（Anki の Desired Retention、既定90%）。実測後。価値: 中
- FSRS を全面採用はしない。個人最適化には数百回以上のレビューが要り（Anki マニュアル）、読者1人ではそこに届かない。固定段には SM-2 のような病理が無く、置き換えの得が小さい

### 借りないと決めたもの（理由つき）

- OpenEvidence の EvidenceGrade（AI が根拠の強さを採点）。「信頼できる AI 検索」の売り方そのもので、競合調査の結論に反する
- 選択肢式と誤答の自動生成（Quizionary・Recall の選択形式）。却下済み
- 学習ヒートマップ・連続記録（Anki Review Heatmap・Screvi のストリーク）。却下済み
- 実名 Q&A（Antaa QA・ヒポクラ）。却下済み。借りるのは期待値の数と工程だけ
- Notion Forms。本文ブロックを作れない
- 「整理しない」思想の全部（mymind）。出典必須は崩さない。借りるのは分類を求めないことだけ

### 調査で確認した MediNode の独自性

主張1行を検索の単位にする製品は既にある（scite の Citation Statement Search、Consensus の抜き書き）。ただしそれらの1行は「論文にそう書いてあった」記録で、人が正しさを引き受けた主張ではない。
Doximity は人が引き受けるが1.2万人の集合で、誰が引き受けたかは特定できない。
MediNode の段0の独自性は、返す1行が「特定の1人が原文と突き合わせて確信度を付けた主張」であることと、無いときに生成で埋めず「棚に無い」と言い切ることの組み合わせにある。
