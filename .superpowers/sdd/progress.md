# Recall 定着エンジン 実装の進み

Plan: docs/superpowers/plans/2026-09-02-recall-engine.md
Spec: docs/superpowers/specs/2026-09-02-recall-engine-design.md
Worktree: .claude/worktrees/recall-engine / branch feat/recall-engine
Base: 58bd114 (local main)

## 事前に見つけた論点（最終レビューで判断する）
- Task 8 `nextDue` は引数 `now` を使わず `void now` で捨てる（計画のコード通り）。未使用引数として指摘されうる
- Task 5 Step 4 は特定ケースで期待値の方を直してよいと書いている。直すときは理由を報告に残す
- 実コーパスの穴の件数（360〜400／600〜700）は NOISE に効果量が入った分だけ基準より減る可能性がある。外れたら原因を見る

## 既知の先行不具合（この実装とは無関係）
- `src/lib/__tests__/admin-engagement-route.test.ts:76` は JST 00:00〜09:00 の間だけ落ちる（テストがUTC・ルートがJST日付キー）。別セッションで修正予定

## 進み
- Task 1: complete (commits 58bd114..53303d9, review clean)
  - Minor（最終レビューで判断）: 1) `process.env = {...ENV}` の一括復元は vi.stubEnv が安全 2) `RECALL_GA` を明示的に削除する beforeEach が無い 3) isRecallEnabled のテストが1つの it に3ケース 4) recall-flag.ts は4本目の同型コピー（共通化の余地） 5) FEATURE_LABELS に recall が無いまま ledger PATCH は feature=recall を受け付ける（管理者＝オーナーなので実害は無いが意図の明示が要る） 6) ledger/route.ts:722 のコメント「既知の3機能」が古い（先行不具合） 7) vitest.config.ts の exclude が `**/.worktrees/**` で、この worktree のパス `.claude/worktrees/` に当たらない
- Task 2: complete (commits 53303d9..0abfc32, review clean → Important 1件をオーナー判断で修正済み)
  - **決定（オーナー）**: `recall_claims` に select ポリシーを置かない。RLS は全拒否のまま、読み取りは service_role のみ。
    → **Task 9 の `/api/recall/claims` は `createClient()` ではなく `createAdminClient()` で `recall_claims` を読むこと。**
    `recall_progress` と `recall_section_reads` は計画どおり本人の RLS 下（`createClient()`）で読む
  - Minor（最終レビューで判断）: 1) `recall_claims_active_idx` は boolean 単独の索引で効かない（`(primary_genre) where active` の方が効く） 2) `updated_at` の default は upsert の衝突時に更新されない（ルート側で明示的に入れる必要がある。Task 6・9・10 で確認） 3) `confidence`・`cloze_status`・`last_result`・`result` に check 制約が無い 4) `for all` は delete も許す（設計は select/insert/update） 5) `genre_slot` の 0..63 が設計書の「席番号（1〜）」と食い違う（実装は 0..63 が正） 6) `user_id` に auth.users への FK が無い（0028 と同じ作法） 7) README の 0027 欠番注記が既存の ※4 と内容が重なる
- Task 3: complete (commits 0abfc32..5da228c, review clean。Important 3件を修正して再レビュー通過)
  - **計画からの意図的な逸脱**: `primaryGenreOf` は canonicalGenreKey が空になる facet（'05.' など）も飛ばす。計画のコードは INBOX しか飛ばさず、そのぶん本来の次のジャンルではなく その他 に落ちていた。`src/lib/genre.ts` の `mergeGenreKeys` と同じ扱いに揃えた
  - 席の正規化キーが重複したらモジュール読み込み時に例外を投げる（黙って席を奪わせない）
  - Minor（最終レビューで判断）: 1) 重複検査のテストが canonicalGenreKey を import せず正規表現を書き写している 2) 例外文が衝突相手の既存席を書いていない 3) コメントの括弧が半角に変わっている箇所がある
  - 申し送り: 設計書 `2026-09-02-recall-engine-design.md:226` の `genre_slot`「席番号（1〜）」は古い。実装・migration・設計書:104 はすべて 0 始まり
- Task 4: complete (commits 5da228c..30fd66a, review clean。Important 2件を修正して再レビュー通過)
  - **実データで見つかった不具合**: 先頭のマークしか見ていなかったため「⚠️❓」の行が確信度 caut の主張として出ていた（実コーパスに1件）。❓ が行のどこかにあれば主張にしない規則に直した。実コーパスの主張数 688→687（ok 423／caut 178／essentials 86）
  - `claimIdOf` はページIDも正規化する（ダッシュの有無で全IDが変わると読者の記録が全部宙に浮く）
  - **Task 5 に持ち越した Important**: `extract-claims.ts` は `pageId: src.pageId` を生のまま主張に入れている。`RecallSectionRead` がこのIDで突き合わせるので `normalizePageId` を通す（Task 5 の dispatch に含めた）
  - Minor（最終レビューで判断）: 1) 実コーパスの ok/caut/essentials の帯が緩く、1規則の壊れは捕まらない 2) `normalizePageId` は `settings.ts` の `normalizeNotionId` と同じ内容の二重定義 3) ❓ を CQ のページ種別記号として本文で使う日が来たら過剰に落とす 4) マーク先頭の行3件が黙って落ちる（既存挙動・テストで固定済み） 5) 表（table_row）の中の記述は主張にならない 6) heading_1 は節として扱われない
- Task 5: complete (commits 30fd66a..5c597e9, review clean。Critical 2件・Important 3件を修正して再レビュー通過)
  - **実データで見つかった不具合**: 信頼区間・治験の発生率が穴になっていた（644穴のうち約20）。範囲の後半だけを伏せる穴が5件。穴の末尾に空白が入る穴が6件。実コーパスは 375claims/644holes → **367claims/627holes**（どちらも計画の帯の中）。`97〜100%` のような本来の臨床レンジは復活した
  - NOISE 13規則のうち12は削除するとテストが赤くなることを実測で確認済み（`第N版` だけは構造上不可能）
  - **Task 4 から持ち越した Important を解消**: `pageId` は `normalizePageId` を通して主張に入る
  - 残っている研究数値の穴（設計上の許容・最終レビューで判断）: 発生率 `X%（n/N）` 23、群間比較 `X% vs Y%` 38、平均・中央値・差 13、追跡期間 18、感度/特異度 22。**受け皿は Task 14 の承認一覧**（`cloze_status` は既定 pending で、承認するまで伏せ字カードにならない）。ただしその篩はまだ作られていない
  - Minor（最終レビューで判断）: 1) I3 の後読みの文字クラス `[〜~～–—]` と範囲規則の `[〜~–—]` が食い違う（`～` と ASCII ハイフン。実データには出ていない） 2) 件数規則がカンマ無しの4桁（`2645例`）を取り逃す 3) ラベルの無い点推定値（`−5.5%（95%CI …）`）は露出したまま
- Task 6: complete (commits 5c597e9..40b19a0, review clean。Critical 3件・Important 5件を修正して再レビュー通過)
  - **本番の同期を壊しかけていた3点を修正**: 1) `recall_claims` への保存が失敗すると同期全体が 500 になり、Algolia は書けているのに読む画面のキャッシュだけ古いまま残っていた（0029 未適用の今、毎晩そうなるところだった）→ 保存の失敗は記録して同期は続ける 2) 非活性化のクエリが主張IDを全部並べて約22,800字になり、リクエスト行の上限（通常8KB）を3倍超えていた → この同期で付けた `updated_at` で選ぶ 3) Notion の取得が一部失敗しても残りを全部 inactive にしていた → 1ページでも取りこぼしたら非活性化そのものを行わない（子ブロックの取得失敗も数える）
  - `{ count: 'exact' }` は計画のコードが正しく計画のテストが誤り。テストの方を直した。非活性化の件数は `SyncResult.synced.recallDeactivated` に出す
  - **オーナーへの申し送り（重要）**: 入れ子の箇条書きを展開（`expandChildren`）しているのは 💡ナレッジ のページだけ。❓CQ・📚Essentials・📄精読ノート の入れ子の主張は今の同期では取れない。実測（Task 6 Step 5）で件数が足りなければ、全ページ展開に変える追加コミットが要る
  - Minor（最終レビューで判断）: 1) 子ブロックの取得上限（1ページ8件）に当たったページは「失敗」に数えないため、上限に押し出された入れ子の主張は静かに inactive になりうる（次の同期で戻る） 2) `failedPages` は同じページで2回増えることがある（`=== 0` としか比べないので実害なし） 3) `updated_at` に索引が無い 4) Recall の障害はログ1行だけで、通知は無い
- Task 7: complete (commits 40b19a0..6d7bfa2, review clean)
  - **計画のテストが計画のコードで通らなかった**（唯一の実測での不一致）。300件中200件を1つの席に寄せる筋書きだったため、その席は球面の2/3を占め、自席の中心との内積は構造上ゼロ近くになる。つまりあの検査は「まとまり」ではなく「席の大きさ」を測っていた。代わりに、席の占有率で正規化した密度（理想値 (1−f)² の半分以上）で検査する。壊した配置3種で実際に落ちることを確認済み
  - **オーナーへの申し送り（設計の判断が要る）**: 球の「場所を覚えられる」という約束は**区画には成り立つが、個々の主張には成り立たない**。
    ・ジャンルの区画そのものは、主張が1割消えても新ジャンルが増えても最大10度しか動かない（学習した「循環はあのへん」は保つ）
    ・一方、格子は主張の総数から作るので、**総数が1つでも変われば全点が引き直される**。30件足すと既存の主張の半分が16度以上動く。総数が変わらない編集（10件出て10件入る）なら86%が同じ座標に留まる
    ・個々の主張を固定したいなら、格子を総数から切り離す（固定サイズの格子に主張を入れる）必要がある。これは配置アルゴリズムの差し替えで、設計上その層は交換可能に作ってある。**Task 15 の実測でオーナーが判断する**
  - もう1つ実測で分かったこと: 主張数の多い上位4ジャンルは、まとまってはいるが席の中心から最大109度離れた場所に置かれる（貪欲な容量つき最近傍の性質。小さい席が先に自席の周りを埋めるため）。席番号は「集まる場所」を決めるが「席の中心に集まる」ことは保証しない。コードのコメントに明記済み
  - 実コーパス687件・15席の配置に 7.4ms。1,500件までは主線で問題なし、3,000件を超えると数フレーム分かかる
- Task 8: complete (commits 6d7bfa2..362fc70, review clean。Important 2件・Minor 4件を修正して再レビュー通過)
  - **計画のコードに2つの不具合**: 1) `nextDue` が引数 `now` を捨てていたため、期限切れが溜まると「もう過ぎた日付」を次の期限として出し、件数も最も古い1日分しか数えなかった → 過ぎている主張があればそれを今の期限として全件数える（戻り値に `overdue` を足した） 2) 「同じ日」の判定が UTC の暦日で、日本時間の 0〜9時に数え違えていた → 既存の `jstDateKey`（`src/lib/admin-daily.ts`）を再利用
  - その JST 修正で `nextDue` が例外を投げる経路ができたので塞いだ（日時として読めない記録が1件あると画面のパネル全体が落ちる状態だった）。`remainingOf` の NaN も 0 に倒す（隠すのではなく「すぐ確かめる」側に出す）
  - テストは全て固定のタイムスタンプで書かれており、TZ=UTC・Asia/Tokyo・Pacific/Kiritimati で同じ結果になることを確認済み（このリポジトリの既知のJSTバグと同じ轍を踏んでいない）
  - Minor（最終レビューで判断）: 1) `newProgress` の初期間隔1日と最初の「覚えた」の1日が続くため、実際の段は 1,1,3,7,… になる（計画どおり） 2) `srs.ts` が `@/lib/admin-daily` に依存する（共有の `jst.ts` に切り出す余地） 3) 外した記録を再び残すときに `removedAt` を戻す経路はこの層に無い（Task 10 の keep ルートが担う）
- Task 9: complete (commits 362fc70..d83bf9d, review clean。Important 2件・Minor 4件を修正して再レビュー通過)
  - **オーナー決定を反映**: `/api/recall/claims` は `createAdminClient()`（service_role）で読む。`recall_claims` は RLS 全拒否なので、`requireRecall()` が主張の全文と外界の間に立つ唯一の関門になる
  - **機能を持たない人に存在が漏れていた**: Next は GET しか書かれていない route でも OPTIONS に 204 と `Allow` ヘッダを自動で返し、そのときガードは走らない。1リクエストで存在が分かる状態だった → 7メソッドすべてを本文なしの 404 で塞ぐ（共有ヘルパを `guard.ts` に置いた）
  - テストのモックが `.eq()` の引数を捨てていたため、`.eq('active', true)` も `.eq('user_id', …)` も消してテストが通っていた → 引数を記録して検査する。消すと赤くなることを実測で確認済み
  - `active` の既定を「有効」から「無効」に倒す／DBのエラー文をそのまま返さない／本人データに `private, no-store`／service_role の客体はガード経由でのみ受け取る
  - 残る漏れ（オーナー判断・実害小）: 未承認の GET は本文なし404、存在しない経路は Next の HTML 404 なので、応答の形が違う。分かるのは「その経路に処理がある」の1ビットだけで、データには届かない。完全に消すにはアプリ全体の `not-found.tsx` が要る
  - **Task 10 への申し送り**: 書き込みルートは本物の `POST` を持つので、Task 9 の1行の再エクスポートとは同居できない（重複エクスポートでビルドが落ちる）。手書きに切り替えたときに OPTIONS を書き忘れると同じ穴が開く。Task 10 で「recall の全 route が7メソッドを塞いでいる」ことを検査するテストを足す
- Task 10: complete (commits d83bf9d..32e97d0, review clean。Minor 5件を修正して再レビュー通過)
  - **計画のテスト用データが計画のコードと合っていなかった**（2か所目の実測での不一致）。`streak: 1, interval_days: 3` から「覚えた」で 7日 になる想定だったが、`applyResult` では 3日 になる。データの側（streak を 2 に）を直して、計画が意図した 7日 と `interval_before: 3 / interval_after: 7` の両方が成り立つようにした
  - 塞いだメソッドの検査が「404 を返すこと」しか見ておらず、204 と Allow を返す OPTIONS でも通っていた（その検査を書いた理由そのものが素通りする状態）→ 共有の拒否関数と同一であることを検査する。実際に 204 を書いて赤くなることを確認済み
  - 外した直後に「覚えた」が届くと、外す前の状態が書き戻されて主張が復活しうる → 外した主張の確認は「残していない」と同じ 404 にする。読んでから書くまでの競合そのものは DB 側の条件付き更新が要るので範囲外（コメントに明記）
  - IDの空文字・長大な文字列がそのまま DB に入っていた（テーブルに外部キーが無いため）→ trim・空を弾く・128字上限（実際のIDは24字・UUIDは36字・`sec0` 数字なので3〜4倍の余裕）
  - テストのモックがテーブル名を捨てていたため、違うテーブルに書いても通っていた。確認ルートの本体 upsert は検査すらされていなかった → どちらも修正
  - 404 の使い分けを確定: **本文なし404＝機能を持たない人への隠蔽（ガード前）／JSON の404＝ガードを通った人へのアプリの状態**。後者に届くのは既に機能を持つ本人だけで、存在しない主張IDと「残していない主張」は同じ応答になるので、主張の在否は漏れない
- Task 11: complete (commits 32e97d0..cf3e378, review clean。Important 5件・Minor 4件を修正して再レビュー通過)
  - **触った主張と選ばれる主張がずれていた**: 当たり判定が「描いた位置」ではなく揺れる前の位置を見ており（約12pxのずれ・点の間隔は約21px）、さらに画面上の距離だけで選ぶため、奥に描かれた点が手前の点より先に選ばれることがあった → 描画と当たり判定を同じ helper に通し、手前の点を優先する
  - **「記憶の残り＝明るさ」が手前側で見えなくなっていた**: 明るさが上限に張り付き、寄って見ているとき（zoom>1.4）に球の手前およそ1/5では、記憶が満タンでも空でも同じ明るさで描かれていた。上限のあとに残りを掛ける形に直し、大きさにも信号を載せた。差は「全く無し」から**どの奥行きでも1.82倍**になった
  - 動きを減らす設定のとき、薄れかけの主張のゆらぎだけ止まっていなかった（binding な制約違反）
  - 保存の直後に古い読み込みが返ると、答えた内容が答える前の状態に上書きされうる → 応答に通し番号を付けて最新だけ反映し、離脱時に中断する
  - 画面の座標（半径・中心）が3か所で別々に計算され、原点も食い違っていた → `viewport()` 1本に統一（Task 12 が4か所目を書かずに済む）
  - **Task 12 への申し送り**: 1) `pickAt` は最後に描いたフレームと同じ `t`・`reduced` を渡すこと（引数が数値4つ並びなので取り違えても型で気付けない） 2) `keep`/`review` は例外を投げるので呼び出し側で `.catch()` する 3) 保存が読み込みと重なると読み込みの側が捨てられるので、主張の一覧を更新したいときは保存後に `refresh()` を呼ぶ 4) 座標は必ず `viewport()` から取る
- Task 12: complete (commits cf3e378..f717e26, review clean。Critical 1件・Important 6件・Minor 多数を修正して再レビュー通過)
  - この画面は自動テストが無い（計画では Task 15 の目視が検証手段）。そのぶんレビューを読み込みで厚くした
  - **一度の通信エラーで画面全体が操作不能になっていた**（Critical）: 保存の失敗を全面のオーバーレイで出しており、`pointer-events` も抜けておらず、消える契機も無かった。球は回り続けるのに確かめる・レンズ・戻す・タップが全部死ぬ状態
  - **「覚えた」の保存に失敗しても、成功したかのように山から消していた**（記録は残らないのに主張だけ消える）→ 失敗したら山に戻す
  - 確かめる直後に戻すを押すと、追いかけて発火するタイマーが山を作り直し、暗いまま抜け道が無くなることがあった → タイマーを全部追跡して解除する
  - 動きを減らす設定が、飛ぶ動き・揺れ・慣性回転には効いていなかった（球は毎回5秒ほど回り続けていた）
  - **伏せ字の範囲を DB の値のまま信じていた**: 逆順の範囲だと本文が重複して出て**医学的な主張が壊れて表示される**。範囲外なら答えの無い空欄、配列でなければ画面が落ちる。純関数 `segments.ts` に切り出して整え（並べ直し・重なりの併合・本文の範囲に丸める）、11種の壊れた入力で「つなぎ直すと本文に戻る」ことを検査
  - 確かめるのカードに閉じる手段が無く、カード自体が操作列を覆っていたため、誤って開いた主張も「覚えた／まだ」を押すまで抜けられなかった（意図しない記録が残る）→ 閉じるを置き、Esc でも閉じる。閉じた主張は山に残る
  - **最終レビューで見ること**: 最後の小さな修正コミット `f717e26` は個別の再レビューを通していない（ビルド・全テスト・動線のトレースは取得済み）
- Task 13: complete (commits f717e26..33467f2, review clean。Important 1件を修正)
  - タブは1か所で宣言され3か所で描かれ、中身は2つの描画モードそれぞれにある。全部フラグの下に入っていることを確認済み。`activeTab` は `tabs` に無いIDなら `search` に落ちるので、仮に tab を recall にされても画面は出ない（二重の防ぎ）
  - **Recall を開くと他のタブへ戻れなかった**: 画面が viewport 全面（`fixed inset-0 z-20`）を覆い、`sticky top-0 z-10` のタブの並びごと隠していた。戻る手段も無く、再読み込み（＝タブが検索に戻る）以外に道が無い状態 → `z-0` にしてタブの並びの下に潜らせる（同時に自前の重なり文脈を作るので、カードの `z-30` もタブの上に飛び出さない）
  - 機能を持たない読者にはタブも画面も文字列も一切出ない。`/api/recall/*` への通信も発生しない
  - 補足（オーナー判断・実害なし）: 公開リポジトリなので、`RecallScreen` のコードと文言は静的 import としてクライアントの束に全員分入る。既存の VineScreen と同じ扱い。公開前に束からも隠したいなら `next/dynamic` が要る
- Task 14: complete (commits 33467f2..11be854, review clean。Critical 1件・Important 8件を修正して再レビュー通過)
  - **「直したつもり」が起きる状態だった**（Critical）: パネルがサーバーの応答を一切見ずに画面を書き換えていたため、400 でも通信断でも「穴が外れた」ように見えていた。この画面は誤った伏せ字を止めるための関門なので、これは目的そのものを裏切る不具合 → 応答を確認し、失敗を伝え、成功時はサーバーから読み直す
  - 穴の削除が、画面に出している正規化後の範囲ではなく生の値と突き合わせていたため、保存済みの範囲が重なっていると**何も消えない**（そして 400 が上の不具合で隠れる）→ 画面と同じ範囲から消す
  - **オーナーが直した穴が毎晩の同期で戻っていた**（Important）: 同期は `holes` を無条件に上書きするので、直して承認した伏せ字が翌日には検出結果に戻り、同じ誤りが毎周期よみがえる状態だった。**判断済み（承認・非承認）の主張は同期が穴を書き戻さない**ように変更。未判断の主張は今まで通り新しい検出結果を受け取る
    → これは計画の「holes は検出規則が変わったら更新したいので上書きする」を意図的に狭めたもの。人が直した情報は検出の推測より確かなので、それを毎晩捨てると承認画面の意味が無くなる。理由はコードのコメントに書いた
  - 承認の差し戻し（穴が入れ替わったら `cloze_status` を pending に戻す）を upsert より**先**に行う。後だと、間で失敗したとき「新しい穴に古い承認が付いたまま、次回以降も差分が出ないので永久に直らない」状態になる
  - **「出さない」の表示が実態と違っていた**: rejected にしても読者には想起カードとして出る（止まらない）。文言を実際の判断（伏せ字にするかどうか）に合わせた。挙動は変えていない
  - 監査ログ（`logAdminAction`）を追加。他の管理系の更新ルートは全て残しており、この列は読者に何を出すかを直接決めるので特に必要
  - **オーナーへの申し送り**: 管理画面の一覧はサーバー側の件数上限を掛けていない（PostgREST の既定上限に当たると黙って切れる可能性・未検証）
